{
  "name": "tinyvg2svg",
  "version": "1.0.0",
  "description": "A TinyVG to SVG converter - transform TinyVG vector graphics into standard SVG format",
  "main": "dist/index.js",
  "bin": {
    "tinyvg2svg": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "build:web": "mkdir -p dist-web && esbuild src/web.ts --bundle --minify --outfile=dist-web/app.js --format=esm --target=es2020 && cp web/index.html dist-web/",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "lint": "eslint src/"
  },
  "keywords": [
    "tinyvg",
    "svg",
    "converter",
    "vector-graphics",
    "tvg"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ]
}
